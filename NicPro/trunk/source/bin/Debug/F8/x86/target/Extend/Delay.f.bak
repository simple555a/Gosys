fblock Delay;
attribute(autoscan);
uuid{fd70-e13c-1e73-c029};

var
     	X: float; in; 
     	TR: float; in;
     	TS: bool; in;
     	K: float; in;    	
     	DT: float; in;
     	LT: float; in;     	
     	Y: float; out;

implementation

static float xn[128];
static date due_T;
static bool first_time = true;
static float dt = 0;

void main()
{
	date now;
	now = gettime();

	if(first_time){
		due_T = now;
		first_time = false;
		return;
	}
	
	dt = (float)(now - due_T);
	due_T = now;
	
	
}